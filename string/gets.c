#include<stdio.h>
#include<string.h>
int main(){
    char str[40];
    gets(str);//use to take input with spaces.
    puts(str);// use to print string.
    //or
    //printf("%s",str);
    return 0;
}